---
import '@fontsource/syne/700.css';
import '@fontsource/syne/800.css';
import '@fontsource/hanken-grotesk/400.css';
import '@fontsource/hanken-grotesk/600.css';
import { getCollection, render } from 'astro:content';
import { Image } from 'astro:assets';
import profileImg from '../assets/profile.jpg'; 
import 'swiper/css';
import 'swiper/css/effect-coverflow';
import 'swiper/css/navigation';
import 'swiper/css/pagination'; 

const allProjects = await getCollection('projects');
allProjects.sort((a, b) => a.data.order - b.data.order);
const carouselProjects = [...allProjects, ...allProjects];

const allExperience = await getCollection('experience');
allExperience.sort((a, b) => a.data.order - b.data.order);

const allEducation = await getCollection('education');
allEducation.sort((a, b) => a.data.order - b.data.order);

const ethosEntries = await getCollection('ethos');
const ethosData = ethosEntries[0];
const { Content: EthosContent } = await render(ethosData);

const allServices = await getCollection('services');
allServices.sort((a, b) => a.data.order - b.data.order);
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>Fareed M. Rifaideen | Full-Stack Digital Marketer & Web Developer</title>
    <meta name="title" content="Fareed M. Rifaideen | Full-Stack Digital Marketer & Web Developer" />
    <meta name="description" content="Architecting high-performance digital ecosystems through technical SEO, bespoke WordPress development, and precision Meta Ads." />
  </head>
  <body>
    
    <header class="site-header">
      <div class="header-container">
        <a href="/" class="logo">
          <img src="/favicon.svg" alt="Fareed Logo" />
        </a>
        <nav class="nav-links">
          <a href="https://wa.me/9717775962" class="contact-btn" target="_blank" rel="noopener noreferrer">WhatsApp</a>
          <a href="#contact-modal" class="contact-btn modal-trigger">Let's Talk</a>
        </nav>
      </div>
    </header>

    <section class="cinematic-hero">
      <div class="cinematic-container">
        <div class="intro-text">
          <h1>Fareed M.<br/>Rifaideen</h1>
          <h2 class="dynamic-subtitle-wrapper">I'm a <span class="dynamic-subtitle">Full-Stack Digital Marketer & Web Developer</span></h2>
        </div>
        <div class="scroll-icon-wrapper">
          <img src="/scroll-Icon.svg" alt="Scroll Down" class="scroll-icon" />
        </div>
        <div class="portrait-wrapper">
          <Image src={profileImg} alt="Fareed Profile" class="portrait-img hero-portrait" /> 
        </div>
        <div class="bio-text">
          <p class="bio">
            I build digital marketing systems that make sense for the bottom line. With ten years of experience in financial operations, I treat marketing budgets like business investments.
          </p>
          <p class="bio">
            As a Full-Stack Marketer and Web Developer, I combine technical SEO, custom WordPress development, and targeted ads to build digital spaces that solve problems and generate real returns.
          </p>
        </div>
      </div>
    </section>

    <section class="services-section">
      <div class="container">
        <h2 class="section-title">What I Do</h2>
        
        <div class="terminal-window">
          <div class="terminal-header">
            <div class="terminal-dots">
              <span class="dot red"></span>
              <span class="dot yellow"></span>
              <span class="dot green"></span>
            </div>
            <div class="terminal-title">bash -- fareed@server: ~</div>
          </div>
          
          <div class="terminal-body">
            <div class="terminal-sidebar">
              {allServices.map((service, index) => (
                <button class={`terminal-command ${index === 0 ? 'active' : ''}`} data-target={`service-${service.data.order}`}>
                  > ./{service.data.command}
                </button>
              ))}
            </div>
            
            <div class="terminal-content">
              <div class="typewriter-container">
                <span id="typewriter-output">{allServices[0].data.summary}</span><span class="cursor">_</span>
              </div>
              <div id="terminal-tags" class="tech-tags mt-tags">
                {allServices[0].data.techStack.map(tag => (
                  <span class="tag terminal-tag">{tag}</span>
                ))}
              </div>
            </div>

            {allServices.map(service => (
               <div id={`service-${service.data.order}`} class="hidden" data-pitch={service.data.summary} data-tags={service.data.techStack.join(',')}></div>
            ))}
          </div>
        </div>

      </div>
    </section>

    <section class="projects-section">
      <div class="ambient-glow"></div>
      
      <div class="container">
        <h2 class="section-title">Featured Projects</h2>
        
        <div class="mobile-swipe-prompt">
          <span>&lt; Swipe &gt;</span>
        </div>

        <div class="swiper projects-swiper">
          <div class="swiper-wrapper">
            {carouselProjects.map((project) => (
              <div class="swiper-slide project-card-wrapper">
                <div class="flip-card-inner">
                  
                  <div class="flip-card-front glass-panel">
                    <h3>{project.data.title}</h3>
                    <p>{project.data.summary}</p>
                    <div class="tech-tags">
                      {project.data.techStack.map((tech) => (
                        <span class="tag">{tech}</span>
                      ))}
                    </div>
                  </div>

                  <div class="flip-card-back glass-panel">
                    <img src={project.data.image} alt={`${project.data.title} Logo`} class="project-logo" />
                    <a href={project.data.liveUrl} target="_blank" rel="noopener noreferrer" class="contact-btn view-project-btn">View Live Project</a>
                  </div>

                </div>
              </div>
            ))}
          </div>
          
          <div class="swiper-button-prev custom-arrow"></div>
          <div class="swiper-button-next custom-arrow"></div>
          <div class="swiper-pagination"></div>
        </div>
      </div>
    </section>

    <section class="experience-section">
      <div class="container">
        <h2 class="section-title">Career Journey</h2>
        
        <div class="career-ruler-wrapper">
          <div class="career-ruler-track">
            <div class="career-ruler-line"></div>
            
            {allExperience.map((job) => (
              <div class="ruler-card">
                <div class="ruler-dot"></div>
                <div class="ruler-content glass-panel">
                  <span class="timeline-date">{job.data.date}</span>
                  <h3>{job.data.title}</h3>
                  <h4>{job.data.company}</h4>
                  <p class="problem-statement">{job.data.problemStatement}</p>
                </div>
              </div>
            ))}
          </div>
        </div>

      </div>
    </section>

    <section class="bento-section">
      <div class="container">
        <h2 class="section-title">Arsenal & Education</h2>
        <div class="bento-grid">
          <div class="bento-card skills-card">
            <h3>Technical Stack</h3>
            <div class="tech-tags">
              <span class="tag">Digital Strategy</span>
              <span class="tag">Technical SEO</span>
              <span class="tag">Meta Ads & PPC</span>
              <span class="tag">Google Ads & SEM</span>
              <span class="tag">Data Analytics (GA4)</span>
              <span class="tag">WordPress & WooCommerce</span>
              <span class="tag">PHP</span>
              <span class="tag">E-commerce Optimization</span>
              <span class="tag">AI-Driven Trend Analysis</span>
              <span class="tag">Campaign Performance Monitoring</span>
            </div>
          </div>

          <div class="bento-card cert-card">
            <h3>Professional Memberships</h3>
            <p>Member of Chartered Management Institute (MCMI)</p>
            <span class="bento-meta">CMI, UK</span>
          </div>

          {allEducation.map((edu) => (
            <div class="bento-card edu-card">
              <h3>{edu.data.degree}</h3>
              <p>{edu.data.institution}</p>
              <span class="bento-meta">{edu.data.date}</span>
            </div>
          ))}
        </div>
      </div>
    </section>

    <section class="ethos-section">
      <div class="container">
        <div class="ethos-box">
          <h2>{ethosData.data.title}</h2>
          <div class="ethos-content">
            <EthosContent />
            <a href={ethosData.data.buttonLink} class="contact-btn ethos-btn modal-trigger">{ethosData.data.buttonText}</a>
          </div>
        </div>
      </div>
    </section>

    <footer class="site-footer">
      <div class="container footer-content">
        <p>© {new Date().getFullYear()} Fareed M. Rifaideen. All rights reserved.</p>
        <div class="social-links">
          <a href="https://wa.me/9717775962" target="_blank" rel="noopener noreferrer">WhatsApp</a>
          <a href="https://www.linkedin.com/in/fareed03" target="_blank" rel="noopener noreferrer">LinkedIn</a>
          <a href="https://github.com/fareed-rifaideen-ecom" target="_blank" rel="noopener noreferrer">GitHub</a>
        </div>
      </div>
    </footer>

    <div class="modal-overlay" id="contactModal">
      <div class="modal-box">
        <button class="modal-close" id="closeModal">✕</button>
        <h3 class="modal-title">Start a Conversation</h3>
        <p class="modal-desc">Tell me about the problem you are trying to solve.</p>
        <form name="project-inquiry" method="POST" data-netlify="true" netlify-honeypot="bot-field" class="brutalist-form">
          <p class="hidden">
            <label>Don’t fill this out if you're human: <input name="bot-field" /></label>
          </p>
          <div class="input-group">
            <input type="text" name="name" id="name" required placeholder=" " />
            <label for="name">Your Name</label>
          </div>
          <div class="input-group">
            <input type="email" name="email" id="email" required placeholder=" " />
            <label for="email">Email Address</label>
          </div>
          <div class="input-group">
            <textarea name="message" id="message" rows="4" required placeholder=" "></textarea>
            <label for="message">Project Details</label>
          </div>
          <button type="submit" class="contact-btn submit-btn">Send Inquiry</button>
        </form>
      </div>
    </div>

    <script>
      import gsap from 'gsap';
      import { ScrollTrigger } from 'gsap/ScrollTrigger';
      import Lenis from '@studio-freight/lenis';
      import Swiper from 'swiper';
      import { EffectCoverflow, Navigation, Pagination, Autoplay } from 'swiper/modules';

      gsap.registerPlugin(ScrollTrigger);

      // --- Smooth Scrolling ---
      const lenis = new Lenis({ duration: 1.2, easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)) });
      function raf(time: number) { lenis.raf(time); requestAnimationFrame(raf); }
      requestAnimationFrame(raf);

      // --- Terminal Typewriter Logic ---
      const commands = document.querySelectorAll('.terminal-command');
      const output = document.getElementById('typewriter-output');
      const tagsContainer = document.getElementById('terminal-tags');
      let typeInterval: ReturnType<typeof setInterval>;

      commands.forEach(btn => {
        // Desktop uses hover, Mobile uses tap
        const trigger = window.innerWidth > 768 ? 'mouseenter' : 'click';
        
        btn.addEventListener(trigger, () => {
          if(btn.classList.contains('active')) return; 
          
          commands.forEach(c => c.classList.remove('active'));
          btn.classList.add('active');

          clearInterval(typeInterval);
          if(output) output.textContent = '';
          if(tagsContainer) {
            tagsContainer.classList.add('hidden');
            tagsContainer.innerHTML = '';
          }

          const targetId = btn.getAttribute('data-target');
          const dataNode = document.getElementById(targetId!);
          const pitchText = dataNode?.getAttribute('data-pitch') || '';
          const tags = dataNode?.getAttribute('data-tags')?.split(',') || [];

          let i = 0;
          typeInterval = setInterval(() => {
            if(output) output.textContent += pitchText.charAt(i);
            i++;
            if (i >= pitchText.length) {
              clearInterval(typeInterval);
              if(tagsContainer) {
                tagsContainer.innerHTML = tags.map(tag => `<span class="tag terminal-tag">${tag}</span>`).join('');
                tagsContainer.classList.remove('hidden');
              }
            }
          }, 15); 
        });
      });

      // --- Swiper 3D Carousel (Classic Behavior) ---
      new Swiper('.projects-swiper', {
        modules: [EffectCoverflow, Navigation, Pagination, Autoplay],
        effect: 'coverflow',
        grabCursor: true,
        centeredSlides: true,
        slidesPerView: 'auto',
        loop: true,
        autoplay: { delay: 3500, disableOnInteraction: false, pauseOnMouseEnter: true },
        pagination: { el: '.swiper-pagination', clickable: true },
        coverflowEffect: { rotate: 50, stretch: 0, depth: 200, modifier: 1, slideShadows: true },
        navigation: { nextEl: '.swiper-button-next', prevEl: '.swiper-button-prev' },
        on: {
          touchStart: function () {
            const prompt = document.querySelector('.mobile-swipe-prompt') as HTMLElement;
            if (prompt) prompt.style.opacity = '0';
          }
        }
      });

      let mm = gsap.matchMedia();

      // --- Desktop GSAP ---
      mm.add("(min-width: 768px)", () => {
        // FULLY RESTORED: Original 2-Step Animation & Scroll Duration
        let tl = gsap.timeline({
          scrollTrigger: { trigger: ".cinematic-hero", pin: true, scrub: 1.5, start: "top top", end: "+=300%" }
        });
        
        // Step 1: Text leaves, portrait enters center frame
        tl.to(".intro-text", { opacity: 0, x: -50, duration: 1 }, 0)
          .to(".scroll-icon-wrapper", { opacity: 0, y: 20, duration: 0.5 }, 0)
          .to(".portrait-wrapper", { x: "0vw", duration: 1.2, ease: "power1.inOut" }, 0);
          
        // Step 2: Portrait moves left, bio text enters
        tl.to(".portrait-wrapper", { x: "-25vw", duration: 1.2, ease: "power1.inOut" }, 1.5)
          .to(".bio-text", { opacity: 1, x: 0, duration: 1, ease: "power1.out" }, 1.5);
      });

      // --- Mobile GSAP ---
      mm.add("(max-width: 767px)", () => {
        // FULLY RESTORED: Original 2-Step Animation & Scroll Duration
        let mobileTl = gsap.timeline({
          scrollTrigger: { trigger: ".cinematic-hero", pin: true, scrub: 1.5, start: "top top", end: "+=250%" }
        });
        
        // Step 1: Text leaves, portrait enters center frame
        mobileTl.to(".intro-text", { opacity: 0, y: 30, duration: 1 }, 0)
          .to(".scroll-icon-wrapper", { opacity: 0, duration: 0.5 }, 0)
          .to(".portrait-wrapper", { x: "0vw", duration: 1.2, ease: "power1.inOut" }, 0);
          
        // Step 2: Portrait moves left, bio text enters
        mobileTl.to(".portrait-wrapper", { x: "-35vw", duration: 1.2, ease: "power1.inOut" }, 1.5)
          .to(".bio-text", { opacity: 1, y: 0, duration: 1, ease: "power1.out" }, 1.5);
      });

      gsap.from(".projects-swiper", { opacity: 0, y: 40, duration: 0.8, ease: "power2.out", scrollTrigger: { trigger: ".projects-section", start: "top 85%" } });
      gsap.from(".bento-card", { opacity: 0, y: 40, stagger: 0.15, duration: 0.8, ease: "power2.out", scrollTrigger: { trigger: ".bento-grid", start: "top 85%" } });
      gsap.from(".ethos-box", { opacity: 0, y: 40, duration: 0.8, ease: "power2.out", scrollTrigger: { trigger: ".ethos-section", start: "top 85%" } });

      // --- Contact Modal Logic ---
      const modal = document.getElementById("contactModal");
      const openBtns = document.querySelectorAll(".modal-trigger");
      const closeBtn = document.getElementById("closeModal");

      const openModal = (e: Event) => {
        e.preventDefault();
        lenis.stop(); 
        gsap.to(modal, { opacity: 1, pointerEvents: "auto", duration: 0.4, ease: "power2.out" });
        gsap.fromTo(".modal-box", { y: 50, opacity: 0 }, { y: 0, opacity: 1, duration: 0.5, delay: 0.1, ease: "power2.out" });
      };

      const closeModal = () => {
        gsap.to(modal, { opacity: 0, pointerEvents: "none", duration: 0.4, ease: "power2.in" });
        lenis.start(); 
      };

      openBtns.forEach(btn => btn.addEventListener("click", openModal));
      closeBtn?.addEventListener("click", closeModal);
      modal?.addEventListener("click", (e) => { if (e.target === modal) closeModal(); });

      // --- Dynamic Rotating Subtitle ---
      const subtitles = ["Full-Stack Digital Marketer & Web Developer", "Technical SEO Specialist", "Bespoke WordPress Architect", "Precision Paid Media Strategist", "Data-Driven Growth Engineer"];
      let subIndex = 0;
      const dynamicSubtitle = document.querySelector(".dynamic-subtitle");

      if (dynamicSubtitle) {
        gsap.delayedCall(3, function changeText() {
          gsap.to(dynamicSubtitle, {
            y: -15, opacity: 0, duration: 0.5, ease: "power2.in",
            onComplete: () => {
              subIndex = (subIndex + 1) % subtitles.length;
              dynamicSubtitle.textContent = subtitles[subIndex];
              gsap.fromTo(dynamicSubtitle, { y: 15, opacity: 0 }, { y: 0, opacity: 1, duration: 0.5, ease: "power2.out" });
              gsap.delayedCall(3, changeText);
            }
          });
        });
      }
    </script>
  </body>
</html>

<style is:global>
  :root {
    --bg-color: #050505;
    --text-primary: #F5F5F5;
    --text-secondary: #A3A3A3;
    --accent: #fff;
  }
  body { background-color: var(--bg-color); color: var(--text-primary); font-family: 'Hanken Grotesk', sans-serif; margin: 0; overflow-x: hidden; }
  .container { max-width: 1200px; margin: 0 auto; padding: 0 2rem; position: relative; z-index: 2; }

  /* Header */
  .site-header { position: fixed; top: 0; width: 100%; padding: 2rem 0; z-index: 100; mix-blend-mode: difference; }
  .header-container { max-width: 1200px; margin: 0 auto; padding: 0 2rem; display: flex; justify-content: space-between; align-items: center; }
  .logo img { height: 35px; width: auto; display: block; }
  .nav-links { display: flex; gap: 1rem; align-items: center; }
  .contact-btn {
    font-family: 'Hanken Grotesk', sans-serif; font-weight: 600; color: #fff; text-decoration: none;
    text-transform: uppercase; letter-spacing: 0.05em; border: 1px solid #fff; padding: 0.6rem 1.5rem;
    border-radius: 50px; font-size: 0.85rem; transition: all 0.3s ease; cursor: pointer; background: transparent;
  }
  .contact-btn:hover { background-color: #fff; color: #000; }

  /* Cinematic Section */
  .cinematic-hero { height: 100vh; width: 100vw; overflow: hidden; position: relative; display: flex; align-items: center; justify-content: center; z-index: 1;}
  .cinematic-container { position: relative; width: 100%; max-width: 1200px; height: 100%; margin: 0 auto; display: flex; align-items: center; }
  .intro-text { position: absolute; left: 2rem; z-index: 10; }
  .intro-text h1 { font-family: 'Syne', sans-serif; font-weight: 800; font-size: clamp(3rem, 7vw, 6.5rem); letter-spacing: -0.02em; margin: 0; line-height: 1.1; }
  .dynamic-subtitle-wrapper { font-family: 'Hanken Grotesk', sans-serif; font-weight: 400; font-size: clamp(1rem, 2vw, 1.5rem); color: var(--text-secondary); margin-top: 1rem; display: flex; align-items: center; gap: 0.4rem;}
  .dynamic-subtitle { display: inline-block; color: var(--text-primary); font-weight: 600; }
  
  .scroll-icon-wrapper { position: absolute; bottom: 3rem; left: 2rem; z-index: 10; }
  .scroll-icon { width: 30px; height: auto; opacity: 0.7; animation: bounce 2s infinite; }
  @keyframes bounce { 0%, 20%, 50%, 80%, 100% { transform: translateY(0); } 40% { transform: translateY(-10px); } 60% { transform: translateY(-5px); } }
  .portrait-wrapper { position: absolute; height: 100vh; width: 100vw; top: 0; left: 0; display: flex; align-items: center; justify-content: center; z-index: 1; transform: translateX(35vw); }
  .portrait-img { height: 100%; width: 100%; max-width: 800px; object-fit: cover; object-position: center; transition: filter 1s ease; }
  .bio-text { position: absolute; right: 2rem; width: 450px; z-index: 10; opacity: 0; transform: translateX(50px); }
  .bio { color: var(--text-secondary); font-size: 1.2rem; line-height: 1.7; margin-bottom: 1.5rem; }
  .section-title { font-family: 'Syne', sans-serif; font-size: 3rem; margin-bottom: 4rem; color: var(--text-primary); }

  /* --- Interactive Terminal UI --- */
  .services-section { padding: 4rem 0 8rem 0; position: relative; z-index: 5; }
  .terminal-window { background: #0a0a0a; border: 1px solid #333; border-radius: 12px; overflow: hidden; font-family: monospace; box-shadow: 0 20px 40px rgba(0,0,0,0.8); }
  .terminal-header { background: #151515; padding: 12px 20px; display: flex; align-items: center; border-bottom: 1px solid #333; }
  .terminal-dots { display: flex; gap: 8px; width: 60px; }
  .dot { width: 12px; height: 12px; border-radius: 50%; background: #444; }
  .dot.red { background: #ff5f56; }
  .dot.yellow { background: #ffbd2e; }
  .dot.green { background: #27c93f; }
  .terminal-title { flex-grow: 1; text-align: center; color: #666; font-size: 0.85rem; letter-spacing: 0.05em; padding-right: 60px; }
  
  .terminal-body { display: flex; min-height: 350px; }
  .terminal-sidebar { width: 35%; border-right: 1px solid #333; background: #0c0c0c; display: flex; flex-direction: column; }
  .terminal-command { padding: 1.5rem; color: #666; text-align: left; cursor: pointer; border: none; background: transparent; border-bottom: 1px solid #1a1a1a; font-family: monospace; font-size: 1rem; transition: all 0.3s; }
  .terminal-command:hover, .terminal-command.active { color: #fff; background: #151515; border-left: 3px solid var(--accent); }
  
  .terminal-content { width: 65%; padding: 3rem; display: flex; flex-direction: column; justify-content: center; }
  .typewriter-container { color: #f5f5f5; font-size: 1.2rem; line-height: 1.7; font-family: 'Hanken Grotesk', sans-serif; }
  .cursor { display: inline-block; width: 10px; height: 1.2rem; background: var(--accent); animation: blink 1s step-end infinite; vertical-align: middle; margin-left: 6px; }
  @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
  
  .mt-tags { margin-top: 2rem; display: flex; gap: 0.5rem; flex-wrap: wrap; }
  .terminal-tag { border-color: #333; background: #111; font-family: monospace; font-size: 0.8rem; }
  .hidden { display: none !important; }

  /* Generic Glass Panel */
  .glass-panel { background: linear-gradient(135deg, rgba(25, 25, 25, 0.4) 0%, rgba(10, 10, 10, 0.7) 100%); backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px); border-top: 1px solid rgba(255, 255, 255, 0.2); border-left: 1px solid rgba(255, 255, 255, 0.15); border-bottom: 1px solid rgba(255, 255, 255, 0.02); border-right: 1px solid rgba(255, 255, 255, 0.02); box-shadow: 0 15px 35px rgba(0, 0, 0, 0.8); border-radius: 12px; }

  /* --- 3D Swiper Carousel --- */
  .projects-section { padding: 5rem 0; position: relative; overflow: hidden; }
  .ambient-glow { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 90vw; height: 90vh; border-radius: 50%; background: radial-gradient(circle at 30% 50%, rgba(99, 102, 241, 0.15) 0%, transparent 50%), radial-gradient(circle at 70% 50%, rgba(217, 70, 239, 0.1) 0%, transparent 50%); filter: blur(40px); z-index: 1; pointer-events: none; }
  .mobile-swipe-prompt { display: none; }
  .projects-swiper { width: 100%; padding: 2rem 0 6rem 0; overflow: visible; z-index: 2; }
  .swiper-slide { width: 380px; height: 380px; perspective: 1000px; }
  .flip-card-inner { position: relative; width: 100%; height: 100%; transition: transform 0.8s cubic-bezier(0.4, 0.2, 0.2, 1); transform-style: preserve-3d; }
  .swiper-slide-active:hover .flip-card-inner { transform: rotateY(180deg); }
  .flip-card-front, .flip-card-back { position: absolute; width: 100%; height: 100%; backface-visibility: hidden; display: flex; flex-direction: column; justify-content: flex-start; padding: 2.5rem; }
  .flip-card-back { transform: rotateY(180deg); align-items: center; justify-content: center; text-align: center; }
  .flip-card-front h3 { font-family: 'Syne', sans-serif; font-size: 1.8rem; color: var(--text-primary); margin: 0 0 1rem 0; }
  .flip-card-front p { color: var(--text-secondary); font-size: 1rem; line-height: 1.6; margin: 0; }
  .tech-tags { display: flex; gap: 0.5rem; margin-top: auto; flex-wrap: wrap; }
  .tag { font-family: 'Hanken Grotesk', sans-serif; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; padding: 0.4rem 0.8rem; border: 1px solid rgba(255, 255, 255, 0.15); color: var(--text-secondary); transition: all 0.3s ease; }
  .project-logo { max-width: 80%; max-height: 120px; object-fit: contain; margin-bottom: 2rem; opacity: 0.9; transition: opacity 0.3s ease; border-radius: 8px; }
  .flip-card-back:hover .project-logo { opacity: 1; }
  .view-project-btn { font-size: 0.9rem; padding: 0.8rem 2rem; }
  .custom-arrow { color: #fff; opacity: 0; transition: opacity 0.3s ease; }
  .custom-arrow::after { font-size: 1.5rem !important; font-weight: bold; }
  .projects-section:hover .custom-arrow { opacity: 0.5; }
  .projects-section:hover .custom-arrow:hover { opacity: 1; }
  .swiper-pagination-bullet { background: var(--text-secondary); opacity: 0.5; transition: all 0.3s ease; }
  .swiper-pagination-bullet-active { background: var(--accent); opacity: 1; transform: scale(1.2); }

  /* --- HORIZONTAL CAREER RULER --- */
  .experience-section { padding: 5rem 0; overflow: hidden; }
  .career-ruler-wrapper { width: 100vw; margin-left: calc(-50vw + 50%); position: relative; padding: 2rem 0; overflow-x: auto; scroll-snap-type: x mandatory; scrollbar-width: none; }
  .career-ruler-wrapper::-webkit-scrollbar { display: none; }
  
  .career-ruler-track { display: flex; gap: 4rem; width: max-content; padding: 0 15vw; position: relative; align-items: flex-start; }
  
  .career-ruler-line { position: absolute; top: 1.5rem; left: 0; width: 100%; height: 2px; background: rgba(255,255,255,0.1); }
  
  .ruler-card { width: 380px; scroll-snap-align: center; position: relative; display: flex; flex-direction: column; }
  .ruler-dot { width: 16px; height: 16px; border-radius: 50%; background: #050505; border: 2px solid var(--accent); position: absolute; top: 0.5rem; left: 0; z-index: 2; transition: all 0.3s ease; }
  .ruler-card:hover .ruler-dot { background: var(--accent); transform: scale(1.3); }
  
  .ruler-content { margin-top: 4rem; padding: 2.5rem; display: flex; flex-direction: column; gap: 0.5rem; }
  .timeline-date { color: var(--text-secondary); font-size: 0.9rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; }
  .ruler-content h3 { font-family: 'Syne', sans-serif; font-size: 1.8rem; margin: 0; color: var(--text-primary); }
  .ruler-content h4 { font-family: 'Hanken Grotesk', sans-serif; color: var(--text-secondary); font-size: 1.1rem; font-weight: 400; margin: 0 0 1rem 0; }
  .problem-statement { font-size: 1rem; line-height: 1.6; color: var(--text-secondary); margin: 0; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 1rem; }

  /* Bento Grid */
  .bento-section { padding: 5rem 0; }
  .bento-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem; }
  .bento-card { border: 1px solid #333; padding: 2.5rem; display: flex; flex-direction: column; justify-content: center; transition: border-color 0.3s ease; background: rgba(10,10,10,0.5); backdrop-filter: blur(10px); }
  .bento-card:hover { border-color: var(--text-primary); }
  .skills-card { grid-column: span 2; }
  .bento-card h3 { font-family: 'Syne', sans-serif; font-size: 1.5rem; margin: 0 0 1rem 0; }
  .bento-card p { color: var(--text-secondary); font-size: 1.1rem; line-height: 1.5; margin: 0 0 1rem 0; }
  .bento-meta { font-size: 0.85rem; color: #666; text-transform: uppercase; letter-spacing: 0.05em; margin-top: auto; }

  /* Work Ethos */
  .ethos-section { padding: 5rem 0 10rem 0; }
  .ethos-box { border: 1px solid #333; padding: 4rem; background: rgba(8,8,8,0.8); backdrop-filter: blur(10px); }
  .ethos-box h2 { font-family: 'Syne', sans-serif; font-size: 2.5rem; color: var(--text-primary); margin: 0 0 2rem 0; }
  .ethos-content p { color: var(--text-secondary); font-size: 1.2rem; line-height: 1.7; margin: 0 0 1.5rem 0; max-width: 800px; }
  .ethos-content p:last-child { margin: 0; }
  .ethos-btn { margin-top: 2.5rem; display: inline-block; }

  /* Footer */
  .site-footer { border-top: 1px solid #1a1a1a; padding: 3rem 0; }
  .footer-content { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; }
  .footer-content p { color: var(--text-secondary); font-size: 1rem; margin: 0; }
  .social-links { display: flex; gap: 2rem; }
  .social-links a { color: var(--text-primary); text-decoration: none; font-family: 'Hanken Grotesk', sans-serif; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; font-size: 0.9rem; transition: color 0.3s ease; }
  .social-links a:hover { color: var(--text-secondary); }

  /* Modal */
  .modal-overlay { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(5, 5, 5, 0.85); backdrop-filter: blur(8px); z-index: 1000; display: flex; align-items: center; justify-content: center; opacity: 0; pointer-events: none; }
  .modal-box { background: #050505; border: 1px solid #333; padding: 4rem; width: 90%; max-width: 500px; position: relative; }
  .modal-close { position: absolute; top: 1.5rem; right: 1.5rem; background: none; border: none; color: var(--text-secondary); font-size: 1.5rem; cursor: pointer; transition: color 0.3s ease; }
  .modal-close:hover { color: var(--text-primary); }
  .modal-title { font-family: 'Syne', sans-serif; font-size: 2rem; margin: 0 0 0.5rem 0; }
  .modal-desc { color: var(--text-secondary); font-size: 1rem; margin-bottom: 2rem; }
  .brutalist-form { display: flex; flex-direction: column; gap: 2rem; }
  .hidden { display: none; }
  .input-group { position: relative; }
  .input-group input, .input-group textarea { width: 100%; background: transparent; border: none; border-bottom: 1px solid #333; padding: 1rem 0; color: var(--text-primary); font-family: 'Hanken Grotesk', sans-serif; font-size: 1rem; outline: none; transition: border-color 0.3s ease; resize: none; }
  .input-group label { position: absolute; left: 0; top: 1rem; color: var(--text-secondary); pointer-events: none; transition: all 0.3s ease; font-size: 1rem; }
  .input-group input:focus, .input-group textarea:focus { border-bottom-color: var(--text-primary); }
  .input-group input:focus ~ label, .input-group input:not(:placeholder-shown) ~ label, .input-group textarea:focus ~ label, .input-group textarea:not(:placeholder-shown) ~ label { top: -10px; font-size: 0.75rem; color: var(--text-primary); text-transform: uppercase; letter-spacing: 0.05em; }
  .submit-btn { width: 100%; margin-top: 1rem; font-size: 1rem; padding: 1rem; }

  /* Mobile Adjustments */
  @media (max-width: 768px) {
    .nav-links { gap: 0.5rem; }
    .contact-btn { padding: 0.5rem 1rem; font-size: 0.75rem; }
    .intro-text { bottom: 15%; left: 1.5rem; }
    .intro-text h1 { font-size: clamp(2.5rem, 12vw, 4rem); }
    .scroll-icon-wrapper { left: 50%; transform: translateX(-50%); bottom: 3%; }
    .portrait-wrapper { transform: translateX(35vw); }
    .bio-text { width: 75%; right: 1.5rem; left: auto; text-align: right; transform: translateY(30px); }
    
    /* Mobile Terminal Accordion */
    .terminal-body { flex-direction: column; min-height: auto; }
    .terminal-sidebar { width: 100%; flex-direction: row; border-right: none; border-bottom: 1px solid #333; overflow-x: auto; scrollbar-width: none; }
    .terminal-sidebar::-webkit-scrollbar { display: none; }
    .terminal-command { padding: 1rem 1.5rem; white-space: nowrap; border-bottom: none; border-left: none !important; border-bottom: 3px solid transparent; }
    .terminal-command.active { border-bottom: 3px solid var(--accent); }
    .terminal-content { width: 100%; padding: 2rem; min-height: 250px; }

    .mobile-swipe-prompt {
      display: block; position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
      z-index: 20; color: var(--text-primary); font-family: 'Hanken Grotesk', sans-serif;
      font-weight: 600; text-transform: uppercase; letter-spacing: 0.1em;
      background: rgba(0,0,0,0.6); padding: 0.8rem 1.5rem; border-radius: 50px;
      backdrop-filter: blur(5px); border: 1px solid rgba(255,255,255,0.2);
      pointer-events: none; transition: opacity 0.4s ease;
      animation: pulse 2s infinite alternate;
    }
    @keyframes pulse { 0% { transform: translate(-50%, -50%) scale(1); } 100% { transform: translate(-50%, -50%) scale(1.05); box-shadow: 0 0 15px rgba(255,255,255,0.2); } }

    .swiper-slide { width: 85vw; height: 85vw; }
    
    /* Mobile Career Ruler */
    .ruler-card { width: 85vw; }
    .career-ruler-track { padding: 0 7.5vw; gap: 2rem; }

    .skills-card { grid-column: span 1; }
    .ethos-box { padding: 2rem; }
    .ethos-box h2 { font-size: 2rem; }
    .footer-content { flex-direction: column; text-align: center; }
    .modal-box { padding: 2rem; }
    .modal-title { font-size: 1.5rem; }
  }
</style>
