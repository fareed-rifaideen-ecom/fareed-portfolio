---
// src/pages/index.astro
import { getCollection, render } from 'astro:content';
import { Image } from 'astro:assets';
import profileImg from '../assets/profile.jpg'; 
import BaseLayout from '../layouts/BaseLayout.astro';
import 'swiper/css';
import 'swiper/css/pagination';

const allProjects = await getCollection('projects');
allProjects.sort((a, b) => a.data.order - b.data.order);
const carouselProjects = allProjects;

const allExperience = await getCollection('experience');
allExperience.sort((a, b) => a.data.order - b.data.order);

const allEducation = await getCollection('education');
allEducation.sort((a, b) => a.data.order - b.data.order);

const ethosEntries = await getCollection('ethos');
const ethosData = ethosEntries[0];
const { Content: EthosContent } = await render(ethosData);

let fetchedServices = [];
try {
  fetchedServices = await getCollection('services');
} catch (e) {
  console.warn("Services collection missing or error. Using fallback.");
}

const fallbackServices = [
  { data: { title: "Full-Stack SEO", command: "execute_seo", summary: "I architect comprehensive search engine strategies from the server to the front end. By combining deep technical audits, semantic site structure, and rigorous keyword gap analyses, I drive high-intent organic traffic directly to your bottom line.", techStack: ["Technical SEO", "SEMrush", "Ahrefs", "Schema Markup"], order: 1 } },
  { data: { title: "Data-Driven Marketing", command: "execute_marketing", summary: "Leveraging a decade of background in financial operations, I treat ad budgets like business investments. I engineer precision paid media campaigns across Meta and Google, utilizing AI-driven trend analysis and real-time data to ruthlessly optimize for maximum ROAS.", techStack: ["Meta Ads", "Google Ads", "GA4", "HubSpot"], order: 2 } },
  { data: { title: "Custom Web Development", command: "execute_development", summary: "I craft bespoke, conversion-optimized digital storefronts. Moving beyond basic templates, I combine sleek UI/UX design with custom WordPress, WooCommerce, and PHP backend development to ensure a flawless, lightning-fast user experience across all devices.", techStack: ["WordPress", "WooCommerce", "PHP", "CSS"], order: 3 } }
];

const allServices = (fetchedServices && fetchedServices.length > 0) ? fetchedServices : fallbackServices;
allServices.sort((a: any, b: any) => a.data.order - b.data.order);
---

<BaseLayout 
  title="Fareed M. Rifaideen | Full-Stack Digital Marketer & Web Developer"
  description="Architecting high-performance digital ecosystems. Specializing in comprehensive SEO, bespoke WordPress development, and precision Full-Stack Digital Marketing strategies."
>
    
  <Image src={profileImg} alt="" class="ghost-watermark" id="bg-portrait" loading="lazy" />

  <header class="site-header">
    <div class="header-container">
      <a href="/" class="logo">
        <img src="/favicon.svg" alt="Fareed Logo" />
      </a>
      <nav class="nav-links">
        <a href="https://wa.me/9717775962" class="whatsapp-icon-link" target="_blank" rel="noopener noreferrer" aria-label="WhatsApp">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
            <path d="M17.472 14.38c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.305-.883-.653-1.48-1.459-1.653-1.756-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51h-.57c-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c0-5.445 4.439-9.884 9.884-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.006 5.45-4.439 9.885-9.881 9.885m8.413-18.297A11.815 11.815 0 0012.052 0C5.495 0 .104 5.384.104 11.933c0 2.096.549 4.14 1.595 5.945L0 24l6.335-1.652c1.73.943 3.682 1.44 5.711 1.44h.004c6.558 0 11.956-5.384 11.96-11.937A11.812 11.812 0 0020.464 3.486z"/>
          </svg>
        </a>
        <!-- STRATEGIC INJECTION: Neon Breathe applied to header CTA -->
        <a href="#contact-modal" class="contact-btn modal-trigger glow-breathe">Let's Talk</a>
      </nav>
    </div>
  </header>

  <section class="cinematic-hero">
    <div class="cinematic-container">
      <div class="intro-text">
        <h1>Fareed M.<br/>Rifaideen</h1>
        <h2 class="dynamic-subtitle-wrapper">I'm a <span class="dynamic-subtitle">Full-Stack Digital Marketer & Web Developer</span></h2>
      </div>
      
      <div class="scroll-icon-wrapper">
        <div class="magnetic-target">
          <span class="magnetic-text">Scroll</span>
          <div class="icon-bounce-wrapper">
            <img src="/scroll-Icon.svg" alt="Scroll Down" class="scroll-icon" />
          </div>
        </div>
      </div>

      <div class="portrait-wrapper">
        <Image src={profileImg} alt="Fareed Profile" class="portrait-img hero-portrait" loading="eager" /> 
      </div>
      <div class="bio-text">
        <p class="bio">
          I build digital marketing systems that make sense for the bottom line. With ten years of experience in financial operations, I treat marketing budgets like business investments.
        </p>
        <p class="bio">
          As a Full-Stack Marketer and Web Developer, I combine technical SEO, custom WordPress development, and targeted ads to build digital spaces that solve problems and generate real returns.
        </p>
        <a href="#ethos" class="btn-outline bio-cta">Read My Ethos <span class="bounce-arrow">↓</span></a>
      </div>
    </div>
  </section>

  <section class="services-sticky-section global-padding" id="services">
    <div class="container">
      <div class="sticky-editorial-layout">
        
        <div class="sticky-sidebar">
          <div class="sticky-content">
            <h2 class="section-title">What I Do</h2>
            <p class="sticky-description">
              I engineer digital growth by combining technical precision with targeted marketing strategies. Here is exactly how my brain can make you money.
            </p>
            <!-- STRATEGIC INJECTION: Neon Breathe applied to Services CTA -->
            <a href="#contact-modal" class="btn-solid modal-trigger sticky-cta glow-breathe"><span class="btn-text">Start a Project</span></a>
          </div>
        </div>

        <div class="scrolling-cards-container">
          {allServices.map((service: any, index: number) => (
            <div class="editorial-service-card glass-panel service-anim-card">
              <div class="service-card-header">
                <span class="service-number">0{index + 1}</span>
                <h3 class="service-title">{service.data.title}</h3>
              </div>
              <div class="service-card-body">
                <p class="service-pitch">{service.data.summary}</p>
                <div class="tech-tags">
                  {service.data.techStack.map((tech: string) => (
                    <span class="tag">{tech}</span>
                  ))}
                </div>
              </div>
            </div>
          ))}
        </div>

      </div>
    </div>
  </section>

  <section class="projects-flip-track-section global-padding" id="projects">
    <div class="container">
      <div class="projects-header-wrapper">
        <div class="label-pill">
          <span class="pill-dot"></span>
          <span>Featured Work</span>
        </div>
        <h2 class="massive-projects-title">Engineered for<br/>Growth & Scale.</h2>
      </div>
    </div>

    <div class="swiper flip-projects-swiper">
      <div class="swiper-wrapper">
        {carouselProjects.map((project: any) => (
          <div class="swiper-slide flip-project-card">
            <div class="flip-card-inner">
              
              <div class="flip-card-front glass-panel">
                <h3 class="proj-front-title">{project.data.title}</h3>
                <p class="proj-front-summary">{project.data.summary}</p>
                <div class="tech-tags">
                  {project.data.techStack.slice(0, 3).map((tech: string) => (
                    <span class="tag pill-tag">{tech}</span>
                  ))}
                </div>
              </div>

              <div class="flip-card-back glass-panel">
                <img src={project.data.image} alt={`${project.data.title} Logo`} class="project-logo bw-logo" loading="lazy" decoding="async" />
                <a href={project.data.liveUrl} target="_blank" rel="noopener noreferrer" class="btn-solid view-project-btn"><span class="btn-text">View Live Project</span></a>
              </div>

            </div>
          </div>
        ))}
      </div>
      <div class="swiper-pagination flip-pagination"></div>
    </div>
  </section>

  <section class="experience-section global-padding">
    <div class="container">
      <h2 class="section-title">Career Journey</h2>
      
      <div class="career-ruler-wrapper" id="career-scroll-box">
        <div class="career-ruler-track">
          <div class="career-ruler-line"></div>
          
          {allExperience.map((job: any) => (
            <div class="ruler-card tooltip-trigger" data-tooltip={job.data.problemStatement}>
              <div class="ruler-dot"></div>
              <div class="ruler-content glass-panel">
                <span class="timeline-date">{job.data.date}</span>
                <h3>{job.data.title}</h3>
                <h4>{job.data.company}</h4>
              </div>
            </div>
          ))}
        </div>
      </div>

      <div class="slider-controls-container">
        <input type="range" id="career-slider" class="custom-monochrome-slider" min="0" max="100" value="0" />
      </div>

    </div>
  </section>

  <div id="global-tooltip" class="glass-panel hidden-tooltip">
    <p id="tooltip-text"></p>
  </div>

  <section class="bento-section global-padding">
    <div class="container">
      <h2 class="section-title">Arsenal & Education</h2>
      <div class="bento-grid">
        <div class="bento-card skills-card">
          <h3>Technical Stack</h3>
          <div class="tech-tags">
            <span class="tag">Digital Strategy</span>
            <span class="tag">Technical SEO</span>
            <span class="tag">Meta Ads & PPC</span>
            <span class="tag">Google Ads & SEM</span>
            <span class="tag">Data Analytics (GA4)</span>
            <span class="tag">WordPress & WooCommerce</span>
            <span class="tag">PHP</span>
            <span class="tag">E-commerce Optimization</span>
            <span class="tag">AI-Driven Trend Analysis</span>
            <span class="tag">Campaign Performance Monitoring</span>
          </div>
        </div>

        <div class="bento-card cert-card">
          <h3>Professional Memberships</h3>
          <p>Member of Chartered Management Institute (MCMI)</p>
          <span class="bento-meta">CMI, UK</span>
        </div>

        {allEducation.map((edu: any) => (
          <div class="bento-card edu-card">
            <h3>{edu.data.degree}</h3>
            <p>{edu.data.institution}</p>
            <span class="bento-meta">{edu.data.date}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="ethos-section global-padding" id="ethos">
    <div class="container">
      <div class="ethos-box">
        <h2>{ethosData.data.title}</h2>
        <div class="ethos-content">
          <EthosContent />
          <a href={ethosData.data.buttonLink} class="contact-btn ethos-btn modal-trigger">{ethosData.data.buttonText}</a>
        </div>
      </div>
    </div>
  </section>

  <footer class="site-footer">
    <div class="container footer-content">
      <p>© {new Date().getFullYear()} Fareed M. Rifaideen. All rights reserved.</p>
      <div class="social-links">
        <a href="https://wa.me/9717775962" target="_blank" rel="noopener noreferrer">WhatsApp</a>
        <a href="https://www.linkedin.com/in/fareed03" target="_blank" rel="noopener noreferrer">LinkedIn</a>
        <a href="https://github.com/fareed-rifaideen-ecom" target="_blank" rel="noopener noreferrer">GitHub</a>
      </div>
    </div>
  </footer>

  <div class="modal-overlay" id="contactModal">
    <div class="modal-box">
      <button class="modal-close" id="closeModal">✕</button>
      <h3 class="modal-title">Start a Conversation</h3>
      <p class="modal-desc">Tell me about the problem you are trying to solve.</p>
      <form name="project-inquiry" method="POST" data-netlify="true" netlify-honeypot="bot-field" class="brutalist-form">
        <p class="hidden">
          <label>Don’t fill this out if you're human: <input name="bot-field" /></label>
        </p>
        <div class="input-group">
          <input type="text" name="name" id="name" required placeholder=" " />
          <label for="name">Your Name</label>
        </div>
        <div class="input-group">
          <input type="email" name="email" id="email" required placeholder=" " />
          <label for="email">Email Address</label>
        </div>
        <div class="input-group">
          <textarea name="message" id="message" rows="4" required placeholder=" "></textarea>
          <label for="message">Project Details</label>
        </div>
        <button type="submit" class="contact-btn submit-btn">Initiate Project</button>
      </form>
    </div>
  </div>

  <script>
    import gsap from 'gsap';
    import { ScrollTrigger } from 'gsap/ScrollTrigger';
    import Lenis from '@studio-freight/lenis';
    import Swiper from 'swiper';
    import { Pagination, Autoplay } from 'swiper/modules';

    gsap.registerPlugin(ScrollTrigger);

    // --- Smooth Scrolling ---
    const lenis = new Lenis({ duration: 1.2, easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)) });
    function raf(time: number) { lenis.raf(time); requestAnimationFrame(raf); }
    requestAnimationFrame(raf);

    // --- Dynamic Shrink Island Logic ---
    ScrollTrigger.create({
      trigger: ".services-sticky-section",
      start: 'top 15%',
      toggleClass: { className: 'is-scrolled', targets: '.site-header' }
    });

    // --- Magnetic Scroll Indicator Logic (Desktop Only) ---
    const magTarget = document.querySelector('.magnetic-target');
    const heroSection = document.querySelector('.cinematic-hero');
    
    if (magTarget && heroSection && window.innerWidth > 768) {
      heroSection.addEventListener('mousemove', (e) => {
        const mouseEvent = e as MouseEvent;
        const wrapper = magTarget.parentElement as HTMLElement;
        const rect = wrapper.getBoundingClientRect();
        
        const centerX = rect.left + rect.width / 2;
        const centerY = rect.top + rect.height / 2;
        
        const distX = mouseEvent.clientX - centerX;
        const distY = mouseEvent.clientY - centerY;
        const distance = Math.sqrt(distX * distX + distY * distY);
        
        if (distance < 600) {
          gsap.to(magTarget, {
            x: distX * 0.3,
            y: distY * 0.3,
            rotate: distX * 0.05, 
            duration: 0.4,
            ease: "power2.out"
          });
        } else {
          gsap.to(magTarget, {
            x: 0,
            y: 0,
            rotate: 0,
            duration: 0.8,
            ease: "elastic.out(1, 0.4)"
          });
        }
      });
      
      heroSection.addEventListener('mouseleave', () => {
        gsap.to(magTarget, { x: 0, y: 0, rotate: 0, duration: 0.8, ease: "elastic.out(1, 0.4)" });
      });
    }

    // --- Lenis Anchor Interceptor ---
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        const targetId = this.getAttribute('href');
        if (targetId && targetId !== '#contact-modal' && targetId !== '#') {
          e.preventDefault();
          lenis.scrollTo(targetId, { duration: 1.5, easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)) });
        }
      });
    });

    // --- Ghost Watermark Background Fade ---
    gsap.to("#bg-portrait", {
      opacity: 0.03,
      scrollTrigger: {
        trigger: ".services-sticky-section",
        start: "top 80%", 
        end: "top 20%",
        scrub: true
      }
    });

    // --- 3D Flip Track Carousel Logic ---
    new Swiper('.flip-projects-swiper', {
      modules: [Pagination, Autoplay],
      slidesPerView: 'auto',
      centeredSlides: true,
      spaceBetween: 80, 
      loop: true,
      speed: 1000, 
      autoplay: { 
        delay: 3000, 
        disableOnInteraction: false, 
        pauseOnMouseEnter: true 
      },
      pagination: { 
        el: '.flip-pagination', 
        clickable: true 
      }
    });

    const flipCards = document.querySelectorAll('.flip-project-card');
    flipCards.forEach(card => {
      card.addEventListener('click', () => {
        if (window.innerWidth <= 768) {
          card.classList.toggle('is-flipped');
          flipCards.forEach(otherCard => {
            if (otherCard !== card) otherCard.classList.remove('is-flipped');
          });
        }
      });
    });

    // --- Horizontal Career Slider Logic ---
    const scrollBox = document.getElementById('career-scroll-box');
    const slider = document.getElementById('career-slider') as HTMLInputElement;

    if (scrollBox && slider) {
      slider.addEventListener('input', (e) => {
        const target = e.target as HTMLInputElement;
        const maxScroll = scrollBox.scrollWidth - scrollBox.clientWidth;
        scrollBox.scrollLeft = (Number(target.value) / 100) * maxScroll;
        target.style.setProperty('--val', target.value + '%');
      });

      scrollBox.addEventListener('scroll', () => {
        const maxScroll = scrollBox.scrollWidth - scrollBox.clientWidth;
        if(maxScroll > 0) {
            const val = (scrollBox.scrollLeft / maxScroll) * 100;
            slider.value = val.toString();
            slider.style.setProperty('--val', val + '%');
        }
      });
    }

    // --- Global Interactive Tooltip Logic ---
    const tooltip = document.getElementById('global-tooltip');
    const tooltipText = document.getElementById('tooltip-text');
    const triggerCards = document.querySelectorAll('.tooltip-trigger');

    triggerCards.forEach(card => {
      card.addEventListener('mousemove', (e: Event) => {
        const mouseEvent = e as MouseEvent;
        if (window.innerWidth > 768 && tooltip && tooltipText) {
          tooltipText.textContent = card.getAttribute('data-tooltip');
          tooltip.classList.remove('hidden-tooltip');
          tooltip.style.left = (mouseEvent.clientX + 20) + 'px';
          tooltip.style.top = (mouseEvent.clientY + 20) + 'px';
        }
      });

      card.addEventListener('mouseleave', () => {
        if(tooltip) tooltip.classList.add('hidden-tooltip');
      });

      card.addEventListener('click', () => {
        if (window.innerWidth <= 768 && tooltip && tooltipText) {
          tooltipText.textContent = card.getAttribute('data-tooltip');
          tooltip.classList.remove('hidden-tooltip');
          tooltip.classList.add('mobile-modal');
          setTimeout(() => tooltip.classList.add('hidden-tooltip'), 4000);
        }
      });
    });

    let mm = gsap.matchMedia();

    // --- Desktop GSAP ---
    mm.add("(min-width: 768px)", () => {
      let tl = gsap.timeline({
        scrollTrigger: { trigger: ".cinematic-hero", pin: true, scrub: 1.5, start: "top top", end: "+=300%" }
      });
      tl.to(".intro-text", { opacity: 0, x: -50, duration: 1 }, 0)
        .to(".scroll-icon-wrapper", { opacity: 0, y: 20, duration: 0.5 }, 0)
        .to(".portrait-wrapper", { x: "0vw", duration: 1.2, ease: "power1.inOut" }, 0);
      tl.to(".portrait-wrapper", { x: "-25vw", duration: 1.2, ease: "power1.inOut" }, 1.5)
        .to(".bio-text", { opacity: 1, x: 0, duration: 1, ease: "power1.out" }, 1.5);
      
      tl.to({}, { duration: 1.5 });
    });

    // --- Mobile GSAP ---
    mm.add("(max-width: 767px)", () => {
      let mobileTl = gsap.timeline({
        scrollTrigger: { trigger: ".cinematic-hero", pin: true, scrub: 1.5, start: "top top", end: "+=300%" }
      });
      mobileTl.to(".intro-text", { opacity: 0, y: 30, duration: 1 }, 0)
        .to(".scroll-icon-wrapper", { opacity: 0, duration: 0.5 }, 0)
        .to(".portrait-wrapper", { x: "0vw", duration: 1.2, ease: "power1.inOut" }, 0);
      
      mobileTl.to(".portrait-wrapper", { x: "-15vw", duration: 1.2, ease: "power1.inOut" }, 1.5)
        .to(".bio-text", { opacity: 1, y: 0, duration: 1, ease: "power1.out" }, 1.5);
        
      mobileTl.to({}, { duration: 1.5 });
    });

    const serviceCards = gsap.utils.toArray(".service-anim-card");
    serviceCards.forEach((card) => {
      gsap.from(card as Element, {
        opacity: 0,
        y: 50,
        duration: 0.8,
        ease: "power2.out",
        scrollTrigger: {
          trigger: card as Element,
          start: "top 85%"
        }
      });
    });

    gsap.from(".projects-flip-track-section", { opacity: 0, y: 40, duration: 0.8, ease: "power2.out", scrollTrigger: { trigger: ".projects-flip-track-section", start: "top 85%" } });
    gsap.from(".bento-card", { opacity: 0, y: 40, stagger: 0.15, duration: 0.8, ease: "power2.out", scrollTrigger: { trigger: ".bento-grid", start: "top 85%" } });

    // --- Contact Modal Logic ---
    const modal = document.getElementById("contactModal");
    const openBtns = document.querySelectorAll(".modal-trigger");
    const closeBtn = document.getElementById("closeModal");

    const openModal = (e: Event) => {
      e.preventDefault();
      lenis.stop(); 
      gsap.to(modal, { opacity: 1, pointerEvents: "auto", duration: 0.4, ease: "power2.out" });
      gsap.fromTo(".modal-box", { y: 50, opacity: 0 }, { y: 0, opacity: 1, duration: 0.5, delay: 0.1, ease: "power2.out" });
    };
    const closeModal = () => {
      gsap.to(modal, { opacity: 0, pointerEvents: "none", duration: 0.4, ease: "power2.in" });
      lenis.start(); 
    };

    openBtns.forEach(btn => btn.addEventListener("click", openModal));
    closeBtn?.addEventListener("click", closeModal);
    modal?.addEventListener("click", (e) => { if (e.target === modal) closeModal(); });

    // --- Dynamic Subtitle ---
    const subtitles = ["Full-Stack Digital Marketer & Web Developer", "Technical SEO Specialist", "Bespoke WordPress Architect", "Precision Paid Media Strategist", "Data-Driven Growth Engineer"];
    let subIndex = 0;
    const dynamicSubtitle = document.querySelector(".dynamic-subtitle");

    if (dynamicSubtitle) {
      gsap.delayedCall(3, function changeText() {
        gsap.to(dynamicSubtitle, {
          y: -15, opacity: 0, duration: 0.5, ease: "power2.in",
          onComplete: () => {
            subIndex = (subIndex + 1) % subtitles.length;
            dynamicSubtitle.textContent = subtitles[subIndex];
            gsap.fromTo(dynamicSubtitle, { y: 15, opacity: 0 }, { y: 0, opacity: 1, duration: 0.5, ease: "power2.out" });
            gsap.delayedCall(3, changeText);
          }
        });
      });
    }
  </script>
</BaseLayout>

<style is:global>
  :root {
    --bg-color: #050505;
    --text-primary: #F5F5F5;
    --text-secondary: #A3A3A3;
    --accent: #fff;

    /* THE GOLDEN TYPOGRAPHIC SCALE */
    --step--2: 0.75rem; /* Tech tags, small metadata */
    --step--1: 0.85rem; /* Buttons, pill labels */
    --step-0: 1rem;     /* Standard body, summaries */
    --step-1: 1.15rem;  /* Large body, bios, pitches */
    --step-2: 1.5rem;   /* Card Headers (Projects, Ruler, Bento) */
    --step-3: 2.5rem;   /* Major Sub-headers (Services, Ethos) */
    --step-4: 3rem;     /* Standard Section Titles */
    --step-5: clamp(3rem, 5vw, 4.5rem); /* Hero / Massive Titles */

    /* GLOBAL SECTION SPACING */
    --section-gap-desktop: 8rem;
    --section-gap-mobile: 4rem;
  }

  body { background-color: var(--bg-color); color: var(--text-primary); font-family: 'Hanken Grotesk', sans-serif; margin: 0; overflow-x: hidden; }
  .container { max-width: 1200px; margin: 0 auto; padding: 0 2rem; position: relative; z-index: 2; }

  .global-padding { padding: var(--section-gap-desktop) 0; }

  /* --- HEADER: INITIAL STATE --- */
  .site-header { 
    position: fixed; top: 0; left: 0; width: 100%; 
    padding: 2rem 0; z-index: 100; 
    transition: padding 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    pointer-events: none; 
  }
  
  .header-container { 
    max-width: 1200px; margin: 0 auto; padding: 0 2rem; 
    display: flex; justify-content: space-between; align-items: center; 
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    border-radius: 50px;
    border: 1px solid transparent;
    background: transparent;
    pointer-events: auto; 
  }

  /* --- HEADER: DYNAMIC SCROLL STATE (THE PILL) --- */
  .site-header.is-scrolled {
    padding: 1.5rem 0; 
  }

  .site-header.is-scrolled .header-container {
    max-width: 800px; 
    padding: 0.8rem 1.5rem;
    background: rgba(10, 10, 10, 0.65); 
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.8);
  }

  .logo img { height: 35px; width: auto; display: block; transition: height 0.4s ease; }
  .site-header.is-scrolled .logo img { height: 28px; }

  .nav-links { display: flex; gap: 1rem; align-items: center; }
  
  .whatsapp-icon-link {
    color: var(--text-primary);
    opacity: 0.7;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .whatsapp-icon-link:hover { opacity: 1; transform: scale(1.1); }
  
  .contact-btn {
    font-family: 'Hanken Grotesk', sans-serif; font-weight: 600; color: #fff; text-decoration: none;
    text-transform: uppercase; letter-spacing: 0.05em; border: 1px solid #fff; padding: 0.6rem 1.5rem;
    border-radius: 50px; font-size: var(--step--1); transition: all 0.3s ease; cursor: pointer; background: transparent;
  }
  .contact-btn:hover { background-color: #fff; color: #000; }
  .site-header.is-scrolled .contact-btn { padding: 0.5rem 1.2rem; }

  /* --- NEW: NEON BREATHE AMBIENT GLOW --- */
  .glow-breathe {
    animation: neon-breathe 4s infinite ease-in-out;
  }
  
  @keyframes neon-breathe {
    0%, 100% { box-shadow: 0 0 0 rgba(255, 255, 255, 0); }
    50% { box-shadow: 0 0 20px rgba(255, 255, 255, 0.3), 0 0 8px rgba(255, 255, 255, 0.2); }
  }

  /* Ghost Watermark */
  .ghost-watermark {
    position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%);
    height: 120vh; width: auto; min-width: 100vw; object-fit: cover;
    opacity: 0; mix-blend-mode: luminosity; pointer-events: none; z-index: 0;
  }

  /* Cinematic Section */
  .cinematic-hero { height: 100vh; width: 100vw; overflow: hidden; position: relative; display: flex; align-items: center; justify-content: center; z-index: 1;}
  .cinematic-container { position: relative; width: 100%; max-width: 1200px; height: 100%; margin: 0 auto; display: flex; align-items: center; }
  .intro-text { position: absolute; left: 2rem; z-index: 10; }
  .intro-text h1 { font-family: 'Syne', sans-serif; font-weight: 800; font-size: clamp(3rem, 7vw, 6.5rem); letter-spacing: -0.02em; margin: 0; line-height: 1.1; }
  .dynamic-subtitle-wrapper { font-family: 'Hanken Grotesk', sans-serif; font-weight: 400; font-size: clamp(1rem, 2vw, 1.5rem); color: var(--text-secondary); margin-top: 1rem; display: flex; align-items: center; gap: 0.4rem;}
  .dynamic-subtitle { display: inline-block; color: var(--text-primary); font-weight: 600; }
  
  .scroll-icon-wrapper { position: absolute; bottom: 3rem; left: 2.5rem; z-index: 10; display: flex; justify-content: center; align-items: flex-end; width: 60px; height: 100px; }
  .magnetic-target { display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .magnetic-text { font-family: 'Hanken Grotesk', sans-serif; font-size: 0.7rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.1em; color: var(--text-secondary); opacity: 0.8; }
  .icon-bounce-wrapper { animation: bounce 2s infinite; }
  .scroll-icon { width: 24px; height: auto; opacity: 0.7; }
  
  @keyframes bounce { 0%, 20%, 50%, 80%, 100% { transform: translateY(0); } 40% { transform: translateY(-10px); } 60% { transform: translateY(-5px); } }
  
  .portrait-wrapper { position: absolute; height: 100vh; width: 100vw; top: 0; left: 0; display: flex; align-items: center; justify-content: center; z-index: 1; transform: translateX(35vw); }
  .portrait-img { height: 100%; width: 100%; max-width: 800px; object-fit: cover; object-position: center; transition: filter 1s ease; }
  .bio-text { position: absolute; right: 2rem; width: 450px; z-index: 10; opacity: 0; transform: translateX(50px); }
  .bio { color: var(--text-secondary); font-size: var(--step-1); line-height: 1.7; margin-bottom: 1.5rem; }
  .bio-cta { margin-top: 1rem; }
  
  .section-title { font-family: 'Syne', sans-serif; font-size: var(--step-4); margin-bottom: 4rem; color: var(--text-primary); }

  /* Generic Glass Panel */
  .glass-panel { background: linear-gradient(135deg, rgba(25, 25, 25, 0.4) 0%, rgba(10, 10, 10, 0.8) 100%); backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px); border-top: 1px solid rgba(255, 255, 255, 0.1); border-left: 1px solid rgba(255, 255, 255, 0.1); border-bottom: 1px solid rgba(255, 255, 255, 0.02); border-right: 1px solid rgba(255, 255, 255, 0.02); box-shadow: 0 15px 35px rgba(0, 0, 0, 0.8); border-radius: 16px; }

  /* --- SOLID CTA BUTTONS --- */
  .btn-solid { 
    position: relative; overflow: hidden; font-family: 'Hanken Grotesk', sans-serif; font-weight: 600; text-transform: uppercase; 
    letter-spacing: 0.05em; font-size: var(--step--1); background: #fff; color: #000; 
    padding: 0.8rem 2.5rem; border-radius: 50px; text-decoration: none; 
    border: 1px solid #fff; display: inline-flex; align-items: center; justify-content: center; 
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }
  
  .btn-solid .btn-text { transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1); z-index: 2; position: relative; }
  .btn-solid::after {
    content: '↗'; position: absolute; right: 1.2rem; opacity: 0;
    transform: translate(-10px, 10px); transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    z-index: 2; font-weight: 800;
  }
  .btn-solid:hover { background: transparent; color: #fff; padding-right: 3.2rem; padding-left: 1.8rem; }
  .btn-solid:hover .btn-text { transform: translateX(-5px); }
  .btn-solid:hover::after { opacity: 1; transform: translate(0, 0); }

  .btn-solid::before {
    content: ''; position: absolute; top: 0; left: -100%; width: 50%; height: 100%;
    background: linear-gradient(to right, rgba(0,0,0,0) 0%, rgba(0,0,0,0.15) 50%, rgba(0,0,0,0) 100%);
    transform: skewX(-25deg); animation: liquid-sheen 4s infinite cubic-bezier(0.16, 1, 0.3, 1); z-index: 1;
  }
  .btn-solid:hover::before { display: none; }
  
  @keyframes liquid-sheen {
    0% { left: -100%; }
    20% { left: 200%; }
    100% { left: 200%; }
  }

  /* --- OUTLINE CTA BUTTON --- */
  .btn-outline {
    font-family: 'Hanken Grotesk', sans-serif; font-weight: 600; text-transform: uppercase; 
    letter-spacing: 0.05em; font-size: var(--step--1); background: transparent; color: #fff; 
    padding: 0.8rem 2.5rem; border-radius: 50px; text-decoration: none; 
    border: 1px solid rgba(255,255,255,0.3); display: inline-flex; align-items: center; justify-content: center; 
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .btn-outline:hover { border-color: #fff; background: rgba(255,255,255,0.05); }

  .bounce-arrow { display: inline-block; margin-left: 0.5rem; transition: transform 0.3s ease; }
  .bio-cta:hover .bounce-arrow { animation: gentle-bounce 1s infinite; }
  @keyframes gentle-bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(4px); }
  }

  /* --- Sticky Editorial Services Section --- */
  .sticky-editorial-layout { display: flex; justify-content: space-between; align-items: flex-start; }
  .sticky-sidebar { width: 35%; position: sticky; top: 25vh; }
  .sticky-content { display: flex; flex-direction: column; align-items: flex-start; }
  .sticky-sidebar .section-title { margin-bottom: 1.5rem; }
  .sticky-description { color: var(--text-secondary); font-size: var(--step-1); line-height: 1.7; margin-bottom: 2.5rem; }
  .sticky-cta { display: inline-flex; }

  .scrolling-cards-container { width: 55%; display: flex; flex-direction: column; gap: 4rem; }
  .editorial-service-card { display: flex; flex-direction: column; gap: 2rem; padding: 3.5rem; border-radius: 16px; }
  
  .service-card-header { display: flex; flex-direction: column; gap: 1rem; }
  .service-number { font-family: 'Syne', sans-serif; font-size: 5rem; color: rgba(255, 255, 255, 0.05); font-weight: 800; line-height: 0.8; margin-left: -5px; }
  .service-title { font-family: 'Syne', sans-serif; font-size: var(--step-3); color: var(--text-primary); margin: 0; }
  
  .service-card-body { display: flex; flex-direction: column; gap: 2rem; }
  .service-pitch { color: var(--text-secondary); font-size: var(--step-1); line-height: 1.7; margin: 0; }
  .tech-tags { display: flex; gap: 0.5rem; flex-wrap: wrap; }
  .tag { font-family: 'Hanken Grotesk', sans-serif; font-size: var(--step--2); font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; padding: 0.4rem 0.8rem; border: 1px solid rgba(255, 255, 255, 0.15); color: var(--text-secondary); transition: all 0.3s ease; }

  /* --- 3D FLIP TRACK SECTION --- */
  .projects-flip-track-section { position: relative; z-index: 5; overflow: hidden; }
  
  .projects-header-wrapper { text-align: left; margin-bottom: 4rem; display: flex; flex-direction: column; align-items: flex-start; }
  .label-pill { display: inline-flex; align-items: center; gap: 0.5rem; border: 1px solid rgba(255,255,255,0.2); border-radius: 50px; padding: 0.4rem 1.2rem; font-size: var(--step--1); color: var(--text-secondary); margin-bottom: 1.5rem; }
  .pill-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--text-primary); }
  .massive-projects-title { font-family: 'Syne', sans-serif; font-size: var(--step-5); color: var(--text-primary); margin: 0; line-height: 1.1; }

  .flip-projects-swiper { width: 100%; padding-bottom: 4rem; overflow: visible; }
  
  .flip-project-card { width: 340px; height: 320px; perspective: 1000px; cursor: pointer; z-index: 1; }
  .flip-project-card:hover { z-index: 10; }
  
  .flip-card-inner { position: relative; width: 100%; height: 100%; transition: transform 0.8s cubic-bezier(0.4, 0.2, 0.2, 1); transform-style: preserve-3d; }
  
  @media (min-width: 769px) {
    .flip-project-card:hover .flip-card-inner { transform: rotateY(180deg); }
  }
  
  .flip-project-card.is-flipped { z-index: 10; }
  .flip-project-card.is-flipped .flip-card-inner { transform: rotateY(180deg); }

  .flip-card-front, .flip-card-back { 
    position: absolute; top: 0; left: 0; width: 100%; height: 100%; backface-visibility: hidden; 
    border-radius: 16px; padding: 2rem; display: flex; flex-direction: column; box-sizing: border-box; margin: 0;
  }
  
  .flip-card-front { justify-content: flex-start; }
  .proj-front-title { font-family: 'Syne', sans-serif; font-size: var(--step-2); color: var(--text-primary); margin: 0 0 1rem 0; line-height: 1.2; }
  
  .proj-front-summary { font-size: var(--step-0); color: var(--text-secondary); line-height: 1.6; margin-bottom: 1rem; display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden; }
  .pill-tag { border-radius: 50px; background: #fff; color: #000; border: none; font-weight: 700; font-size: var(--step--2); }
  
  .flip-card-back { transform: rotateY(180deg); align-items: center; justify-content: center; text-align: center; }
  .project-logo { max-width: 80%; max-height: 90px; object-fit: contain; margin-bottom: 2rem; }
  .bw-logo { filter: grayscale(100%) brightness(1.5); transition: all 0.4s ease; opacity: 0.8; }
  .flip-card-back:hover .bw-logo { filter: grayscale(0) brightness(1); opacity: 1; }
  .view-project-btn { width: 100%; margin-top: auto; }

  .flip-pagination { position: absolute; bottom: 0; display: flex; justify-content: center; gap: 8px; }
  .swiper-pagination-bullet { background: var(--text-secondary); opacity: 0.3; transition: all 0.3s ease; width: 8px; height: 8px; }
  .swiper-pagination-bullet-active { background: #fff; opacity: 1; transform: scale(1.3); }

  /* --- HORIZONTAL CAREER RULER --- */
  .experience-section { overflow: hidden; position: relative; }
  .career-ruler-wrapper { width: 100vw; margin-left: calc(-50vw + 50%); position: relative; padding: 2rem 0; overflow-x: auto; scroll-snap-type: x mandatory; scrollbar-width: none; }
  .career-ruler-wrapper::-webkit-scrollbar { display: none; }
  
  .career-ruler-track { display: flex; gap: 3rem; width: max-content; padding: 0 15vw; position: relative; align-items: center; min-height: 250px; }
  .career-ruler-line { position: absolute; top: 50%; left: 0; width: 100%; height: 2px; background: rgba(255,255,255,0.1); transform: translateY(-50%); }
  
  .ruler-card { width: 280px; scroll-snap-align: center; position: relative; display: flex; flex-direction: column; cursor: crosshair; }
  .ruler-dot { width: 16px; height: 16px; border-radius: 50%; background: #050505; border: 2px solid var(--accent); position: absolute; top: 50%; left: -24px; z-index: 2; transition: all 0.3s ease; transform: translateY(-50%); }
  .ruler-card:hover .ruler-dot { background: var(--accent); transform: translateY(-50%) scale(1.3); }
  
  .ruler-content { padding: 1.5rem; display: flex; flex-direction: column; gap: 0.3rem; margin-left: 1rem; }
  .timeline-date { color: var(--text-secondary); font-size: var(--step--1); font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; }
  .ruler-content h3 { font-family: 'Syne', sans-serif; font-size: var(--step-2); margin: 0; color: var(--text-primary); }
  .ruler-content h4 { font-family: 'Hanken Grotesk', sans-serif; color: var(--text-secondary); font-size: var(--step-0); font-weight: 400; margin: 0; }

  /* Custom Range Slider Styling */
  .slider-controls-container { max-width: 600px; margin: 2rem auto 0 auto; padding: 0 2rem; }
  .custom-monochrome-slider {
    -webkit-appearance: none;
    width: 100%;
    background: transparent;
    --val: 0%;
  }
  .custom-monochrome-slider::-webkit-slider-thumb {
    -webkit-appearance: none;
    height: 20px; width: 20px;
    border-radius: 50%;
    background: #fff;
    cursor: pointer;
    margin-top: -8px; 
    box-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
  }
  .custom-monochrome-slider::-webkit-slider-runnable-track {
    width: 100%; height: 4px;
    border-radius: 2px;
    background: linear-gradient(to right, #fff var(--val), #333 var(--val));
  }

  /* Global Interactive Tooltip */
  .hidden-tooltip { opacity: 0; pointer-events: none; visibility: hidden; }
  #global-tooltip { position: fixed; z-index: 9999; padding: 1.5rem; max-width: 320px; color: #f5f5f5; font-size: var(--step-0); line-height: 1.6; transition: opacity 0.2s ease; border-radius: 8px; pointer-events: none; }
  
  /* Bento Grid */
  .bento-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem; }
  .bento-card { border: 1px solid #333; padding: 2.5rem; display: flex; flex-direction: column; justify-content: center; transition: border-color 0.3s ease; background: rgba(10,10,10,0.5); backdrop-filter: blur(10px); }
  .bento-card:hover { border-color: var(--text-primary); }
  .skills-card { grid-column: span 2; }
  .bento-card h3 { font-family: 'Syne', sans-serif; font-size: var(--step-2); margin: 0 0 1rem 0; }
  .bento-card p { color: var(--text-secondary); font-size: var(--step-0); line-height: 1.5; margin: 0 0 1rem 0; }
  .bento-meta { font-size: var(--step--1); color: #666; text-transform: uppercase; letter-spacing: 0.05em; margin-top: auto; }

  /* Work Ethos */
  .ethos-box { border: 1px solid #333; padding: 4rem; background: rgba(8,8,8,0.8); backdrop-filter: blur(10px); }
  .ethos-box h2 { font-family: 'Syne', sans-serif; font-size: var(--step-3); color: var(--text-primary); margin: 0 0 2rem 0; }
  .ethos-content p { color: var(--text-secondary); font-size: var(--step-1); line-height: 1.7; margin: 0 0 1.5rem 0; max-width: 800px; }
  .ethos-content p:last-child { margin: 0; }
  .ethos-btn { margin-top: 2.5rem; display: inline-flex; }

  /* Footer */
  .site-footer { border-top: 1px solid #1a1a1a; padding: 3rem 0; }
  .footer-content { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; }
  .footer-content p { color: var(--text-secondary); font-size: var(--step-0); margin: 0; }
  .social-links { display: flex; gap: 2rem; }
  .social-links a { color: var(--text-primary); text-decoration: none; font-family: 'Hanken Grotesk', sans-serif; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; font-size: var(--step--1); transition: color 0.3s ease; }
  .social-links a:hover { color: var(--text-secondary); }

  /* Modal */
  .modal-overlay { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(5, 5, 5, 0.85); backdrop-filter: blur(8px); z-index: 1000; display: flex; align-items: center; justify-content: center; opacity: 0; pointer-events: none; }
  .modal-box { background: #050505; border: 1px solid #333; padding: 4rem; width: 90%; max-width: 500px; position: relative; }
  .modal-close { position: absolute; top: 1.5rem; right: 1.5rem; background: none; border: none; color: var(--text-secondary); font-size: var(--step-2); cursor: pointer; transition: color 0.3s ease; }
  .modal-close:hover { color: var(--text-primary); }
  .modal-title { font-family: 'Syne', sans-serif; font-size: var(--step-3); margin: 0 0 0.5rem 0; }
  .modal-desc { color: var(--text-secondary); font-size: var(--step-0); margin-bottom: 2rem; }
  .brutalist-form { display: flex; flex-direction: column; gap: 2rem; }
  .hidden { display: none; }
  .input-group { position: relative; }
  .input-group input, .input-group textarea { width: 100%; background: transparent; border: none; border-bottom: 1px solid #333; padding: 1rem 0; color: var(--text-primary); font-family: 'Hanken Grotesk', sans-serif; font-size: var(--step-0); outline: none; transition: border-color 0.3s ease; resize: none; }
  .input-group label { position: absolute; left: 0; top: 1rem; color: var(--text-secondary); pointer-events: none; transition: all 0.3s ease; font-size: var(--step-0); }
  .input-group input:focus, .input-group textarea:focus { border-bottom-color: var(--text-primary); }
  .input-group input:focus ~ label, .input-group input:not(:placeholder-shown) ~ label, .input-group textarea:focus ~ label, .input-group textarea:not(:placeholder-shown) ~ label { top: -10px; font-size: var(--step--2); color: var(--text-primary); text-transform: uppercase; letter-spacing: 0.05em; }
  .submit-btn { width: 100%; margin-top: 1rem; font-size: var(--step-0); padding: 1rem; }

  /* Mobile Adjustments */
  @media (max-width: 768px) {
    :root {
      --step-2: 1.3rem; 
      --step-3: 2rem;   
      --step-4: 2.5rem; 
    }

    .global-padding { padding: var(--section-gap-mobile) 0; }

    .site-header { padding: 1rem 0; }
    .site-header.is-scrolled { padding: 1rem 0; }
    .site-header.is-scrolled .header-container { max-width: 90vw; padding: 0.6rem 1.2rem; }

    .nav-links { gap: 0.5rem; }
    .contact-btn { padding: 0.5rem 1rem; }
    .site-header.is-scrolled .contact-btn { padding: 0.4rem 1rem; }

    .intro-text { bottom: 15%; left: 1.5rem; }
    
    .scroll-icon-wrapper { left: 50%; transform: translateX(-50%); bottom: 12%; }
    .magnetic-target { transform: none !important; }
    
    .portrait-wrapper { transform: translateX(35vw); }
    
    .bio-text { 
      width: 85%; 
      right: auto; 
      left: 1.5rem; 
      text-align: left; 
      transform: translateY(30px); 
    }
    
    .sticky-editorial-layout { flex-direction: column; gap: 4rem; }
    .sticky-sidebar, .scrolling-cards-container { width: 100%; }
    .sticky-sidebar { position: relative; top: 0; }
    .scrolling-cards-container { gap: 3rem; padding-bottom: 0; }
    .editorial-service-card { padding: 2.5rem; gap: 1.5rem; }
    .service-number { font-size: 4rem; }

    .flip-project-card { width: 85vw; height: 320px; } 

    .ruler-card { width: 75vw; }
    .career-ruler-track { padding: 0 12.5vw; gap: 2rem; }
    
    .mobile-modal { top: 50% !important; left: 50% !important; transform: translate(-50%, -50%) !important; width: 90vw; pointer-events: auto !important; box-shadow: 0 0 50px rgba(0,0,0,0.9); border-color: rgba(255,255,255,0.3); }

    .skills-card { grid-column: span 1; }
    .ethos-box { padding: 2rem; }
    .footer-content { flex-direction: column; text-align: center; }
    .modal-box { padding: 2rem; }
  }
</style>